{
  "installCommand": "npm install && npm install --prefix project",
  "buildCommand": "npm run build",
  "outputDirectory": "project/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
